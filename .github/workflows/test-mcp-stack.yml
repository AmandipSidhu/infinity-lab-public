name: Test MCP Stack

on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/**'
      - '.github/workflows/test-mcp-stack.yml'
      - 'ARCHITECTURE_v2.9.md'

jobs:
  test-mcp-stack:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Docker
        uses: docker/setup-buildx-action@v3

      - name: Make scripts executable
        run: chmod +x scripts/*.sh

      - name: Create .env.mcp from GitHub Secrets
        run: |
          cat > ~/.env.mcp << EOF
          # Auto-generated from GitHub Secrets
          LINEAR_API_KEY=${{ secrets.LINEAR_API_KEY }}
          QUANTCONNECT_USER_ID=${{ secrets.QC_USER_ID }}
          QUANTCONNECT_API_TOKEN=${{ secrets.QC_API_TOKEN }}
          GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}
          MEMORY_FILE_PATH=$HOME/mcp-data/memory.json
          EOF
          echo "✅ Created ~/.env.mcp from secrets"

      - name: Install MCP dependencies
        run: |
          bash scripts/install_mcp_deps.sh

      - name: Start all MCP servers
        run: |
          bash scripts/start_all_mcps.sh

      - name: Verify health checks
        run: |
          echo "🔍 Testing individual MCP endpoints..."
          
          # Test QuantConnect (8000)
          echo "Testing QuantConnect MCP (port 8000)..."
          curl -f http://localhost:8000/health || { echo "❌ QuantConnect failed"; exit 1; }
          echo "✅ QuantConnect healthy"
          
          # Test Linear (8001)
          echo "Testing Linear MCP (port 8001)..."
          curl -f http://localhost:8001/health || { echo "❌ Linear failed"; exit 1; }
          echo "✅ Linear healthy"
          
          # Test Memory (8002)
          echo "Testing Memory MCP (port 8002)..."
          curl -f http://localhost:8002/health || { echo "❌ Memory failed"; exit 1; }
          echo "✅ Memory healthy"
          
          # Test Sequential Thinking (8003)
          echo "Testing Sequential Thinking MCP (port 8003)..."
          curl -f http://localhost:8003/health || { echo "❌ Sequential Thinking failed"; exit 1; }
          echo "✅ Sequential Thinking healthy"
          
          echo "✅ All health checks passed!"

      - name: Check MCP logs
        if: always()
        run: |
          echo "📋 MCP Server Logs:"
          echo ""
          echo "=== QuantConnect MCP (via Supergateway) ==="
          tail -20 ~/mcp-logs/quantconnect-mcp.log || echo "No QuantConnect logs"
          echo ""
          echo "=== Linear MCP (via Supergateway) ==="
          tail -20 ~/mcp-logs/linear-mcp.log || echo "No Linear logs"
          echo ""
          echo "=== Memory MCP (via Supergateway) ==="
          tail -20 ~/mcp-logs/memory-mcp.log || echo "No Memory logs"
          echo ""
          echo "=== Sequential Thinking MCP ==="
          tail -20 ~/mcp-logs/thinking-mcp.log || echo "No Thinking logs"

      - name: Stop all MCP servers
        if: always()
        run: |
          bash scripts/stop_all_mcps.sh

      - name: Verify clean shutdown
        if: always()
        run: |
          echo "🔍 Verifying all processes stopped..."
          
          # Check if any MCP processes still running
          if pgrep -f "supergateway" > /dev/null; then
            echo "⚠️ Supergateway processes still running"
            pgrep -af "supergateway"
          else
            echo "✅ No Supergateway processes"
          fi
          
          if pgrep -f "sequential-thinking" > /dev/null; then
            echo "⚠️ Sequential Thinking still running"
            pgrep -af "sequential-thinking"
          else
            echo "✅ No Sequential Thinking processes"
          fi
          
          # Note: Docker containers auto-deleted with --rm flag
          if docker ps -a | grep "quantconnect/mcp-server" > /dev/null; then
            echo "⚠️ QuantConnect Docker containers still exist"
            docker ps -a | grep "quantconnect/mcp-server"
          else
            echo "✅ No QuantConnect Docker containers"
          fi
          
          echo "✅ Clean shutdown verified"

      - name: Report results to Linear
        if: always()
        env:
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
        run: |
          # TODO: Add Linear API call to update UNI-50 with test results
          echo "📊 Test Results:"
          echo "  Workflow: ${{ github.workflow }}"
          echo "  Run: ${{ github.run_number }}"
          echo "  Status: ${{ job.status }}"
