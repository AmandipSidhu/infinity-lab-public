# invalid_001_missing_fields.yaml
# Expected errors: SVR-E001, SVR-E002, SVR-E003, SVR-E013, SVR-E014, SVR-E015,
#                  SVR-E016, SVR-E017, SVR-E018
# This spec is missing metadata fields and the entire risk_management and
# performance_targets blocks.

metadata:
  # name is missing — SVR-E001
  # version is missing — SVR-E002
  # description is missing — SVR-E003
  author: "Nobody"

strategy:
  type: "breakout"

  universe:
    symbols:
      - "TSLA"
    resolution: "daily"

  signals:
    entry:
      conditions:
        - "price breaks above 52-week high by 2%"
    exit:
      conditions:
        - "price drops 5% below entry"

  # risk_management block is entirely absent — SVR-E013 (also triggers SVR-E014, SVR-E015)
  # performance_targets block is entirely absent — SVR-E016 (also SVR-E017, SVR-E018)

  backtesting:
    start_date: "2020-01-01"
    end_date: "2024-12-31"
    initial_capital: 50000
    min_trades: 150
    benchmark: "QQQ"

  data_requirements:
    resolution: "daily"
    min_history_days: 260
    indicators:
      - "52-week high"
