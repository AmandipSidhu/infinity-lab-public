# invalid_001_missing_fields.yaml
# Expected errors (new schema):
#   SVR-E001 (metadata.trading_style missing)
#   SVR-E005 (metadata.name missing)
#   SVR-E003 (capital section entirely absent)
#   SVR-E023 (risk_management.stop_loss missing — risk_management absent)
#   SVR-E024 (risk_management.position_sizing missing — risk_management absent)
#   SVR-E021 (acceptance_criteria.max_drawdown_pct missing)
#   SVR-E046 (acceptance_criteria.min_sharpe_ratio missing)
#   SVR-E048 (acceptance_criteria.min_profit_factor missing)
#   SVR-E050 (acceptance_criteria.min_trades missing)

metadata:
  # name is intentionally absent  — SVR-E005
  # trading_style is intentionally absent — SVR-E001
  description: "A strategy with several required fields omitted to exercise error detection."
  author: "Nobody"
  version: "0.1"

# capital section is entirely absent — SVR-E003

data:
  instruments:
    - "TSLA"
  resolution: "daily"
  start_date: "2020-01-01"
  end_date: "2024-12-31"

signals:
  entry:
    - "Price breaks above the 52-week high by 2%"
  exit:
    - "Exit after 10 bars"

# risk_management is entirely absent — SVR-E023, SVR-E024
# acceptance_criteria is entirely absent — SVR-E021, E046, E048, E050
